<h2>User info</h2>
<%= @user.each do |user| %>
<%= attachment_image_tag user, :profile_image, :fill, 80, 80 ,fallback: "no_image.jpg" %>
  <table>
    <tr>
      <th>name</th>
      <th><%= user.name%></th>
    </tr>
    <tr>
      <td>introduction</td>
      <td><%= user.introduction %></td>
  <% end %>
    </tr>
</table>